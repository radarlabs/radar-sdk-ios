name: Xcode - Build, Analyze, & Test

on:
  pull_request:

# Cancel superseded runs when a PR branch is re-pushed
concurrency:
  group: ${{ github.workflow }}-${{ github.head_ref || github.ref }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

# gh (used by the Format Swift step) needs read access to the PR diff.
permissions:
  contents: read
  pull-requests: read

jobs:
  build:
    name: Build and analyse default scheme using xcodebuild command
    runs-on: macos-15

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          submodules: recursive
          # Org level secret
          token: ${{ secrets.SUBMODULESTOKEN }}

      - name: Use Xcode 26.3
        run: sudo xcode-select -switch /Applications/Xcode_26.3.app

      - name: Lint Swift
        run: |
          if ! command -v swiftlint >/dev/null; then
            brew install swiftlint
          fi
          swiftlint lint --strict --baseline .swiftlint-baseline.json

      - name: Format Swift
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          # Ask GitHub which files this PR changed (server-side diff — needs no local git
          # history or merge-base, so it is correct on stacked PRs without a deep checkout).
          # Keep the Swift files that still exist (drop deletions), then let make reformat
          # them and fail if anything changed.
          if ! command -v swift-format >/dev/null; then
            brew install swift-format
          fi
          CHANGED=$(gh pr diff ${{ github.event.pull_request.number }} --name-only)
          FILES=""
          for f in $CHANGED; do
            case "$f" in *.swift) [ -f "$f" ] && FILES="$FILES $f" ;; esac
          done
          make format-check FORMAT_FILES="$FILES"

      - name: Pick an available iPhone simulator
        id: pick_sim
        run: |
          # Get a UDID for a valid iPhone simulator only (exclude iPads, Apple Watch, tvOS, etc)
          SIM_ID=$(xcrun simctl list devices --json \
            | jq -r '
                .devices
                | to_entries
                # Filter keys for iOS runtimes only (ignore tvOS/watchOS)
                | map(select(.key | startswith("com.apple.CoreSimulator.SimRuntime.iOS")))
                | .[]
                .value[]
                # Only devices available AND name contains "iPhone"
                | select(.isAvailable == true and (.name | test("iPhone")))
                .udid
            ' | head -n 1)

          if [ -z "$SIM_ID" ]; then
            echo "No available iPhone simulator found"
            exit 1
          fi

          echo "SIMULATOR_ID=$SIM_ID" >> $GITHUB_ENV
          echo "Picked iPhone simulator UDID: $SIM_ID"

      - name: Build and Analyze
        env:
          SIMULATOR_ID: ${{ env.SIMULATOR_ID }}
          scheme: RadarSDK
        run: |
          make ci-build-analyze CI_DESTINATION="platform=iOS Simulator,id=${SIMULATOR_ID}"

      - name: Test
        env:
          SIMULATOR_ID: ${{ env.SIMULATOR_ID }}
          scheme: RadarSDK
        run: |
          make ci-test-pretty CI_DESTINATION="platform=iOS Simulator,id=${SIMULATOR_ID}"
        
      - name: Test swift
        env:
          SIMULATOR_ID: ${{ env.SIMULATOR_ID }}
          scheme: RadarSDK
        run: |
          make ci-test-swift CI_DESTINATION="platform=iOS Simulator,id=${SIMULATOR_ID}"

      - name: Build example app
        env:
          SIMULATOR_ID: ${{ env.SIMULATOR_ID }}
        run: |
          make ci-build-example CI_DESTINATION="platform=iOS Simulator,id=${SIMULATOR_ID}"

      - name: Lint podspecs
        run: make lint
