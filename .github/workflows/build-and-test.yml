name: Xcode - Build, Analyze, & Test

on:
  push:
    branches: [ master, develop ]
  pull_request:
    branches: [ master, develop ]

jobs:
  build:
    name: Build and analyse default scheme using xcodebuild command
    runs-on: macos-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Build, Analyze, & Test
        env:
          scheme: Framework
        run: |
          xcodebuild clean build analyze test -scheme Framework -workspace Example/Example.xcodeproj/project.xcworkspace -destination 'name=iPhone 12 Pro' | xcpretty && exit ${PIPESTATUS[0]}
